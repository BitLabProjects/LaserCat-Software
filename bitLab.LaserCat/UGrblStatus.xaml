<UserControl x:Class="bitLab.LaserCat.UGrblStatus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:conv="clr-namespace:bitLab.LaserCat.Converters">
  <Grid>
    <Grid.Resources>
      <Style TargetType="TextBlock">
        <Setter Property="FontFamily" Value="SegoeUI" />
        <Setter Property="FontWeight" Value="Light" />
        <Setter Property="VerticalAlignment" Value="Center" />
      </Style>
    </Grid.Resources>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal" Margin="0 3 0 0">
      <TextBlock Text="Planner" FontSize="16" Width="80" />
      <ProgressBar x:Name="progressBar" Minimum="0" Maximum="{Binding PlannerBlockMaxSize, Mode=OneWay}" Value="{Binding PlannerBlockCount, Mode=OneWay}" Width="100" ValueChanged="progressBar_ValueChanged_1" />
      <TextBlock x:Name="txtProgressBar" Margin="3 0 0 0" />
    </StackPanel>
  </Grid>
</UserControl>
