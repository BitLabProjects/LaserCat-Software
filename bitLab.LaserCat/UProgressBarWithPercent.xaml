<UserControl x:Class="bitLab.LaserCat.UProgressBarWithPercent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="300">
  <StackPanel x:Name="stackPanel" Orientation="Horizontal">
    <ProgressBar x:Name="progressBar" Minimum="0" Maximum="{Binding Maximum, Mode=OneWay}" Value="{Binding Value, Mode=OneWay}" Width="100" ValueChanged="progressBar_ValueChanged_1" />
    <TextBlock x:Name="txtProgressBar" Margin="3 0 0 0" />
  </StackPanel>
</UserControl>
